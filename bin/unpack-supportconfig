#!/bin/bash

dir=$(basename "${1%.tbz}")

if [ -d "$dir" ]; then
    echo "$dir already exists"
    echo
else
    up "$1" || exit $?
fi

if ! cd "$dir"; then
    echo >&2 "Expected directory $dir was missing"
    exit 1
fi

split-supportconfig *.txt
